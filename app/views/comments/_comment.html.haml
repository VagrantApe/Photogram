.user-name
  = comment.user.user_name
.comment-content
  = comment.context
- if comment.user == current_user
  = link_to post_comment_path(post, comment), method: :delete, remote: true, data: { confirm: "Are you sure?" } do
    %span(class="glyphicon glyphicon-remove delete-comment")
